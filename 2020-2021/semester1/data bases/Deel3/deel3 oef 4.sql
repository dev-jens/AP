/*UPDATE boetes 
SET 
    `bedrag` = `bedrag` * 1.05
WHERE
    `betalingsnr` > 0;*/
/* UPDATE boetes 
SET 
    `bedrag` = 120
WHERE
    `datum` > '2020-01-01'
        AND `datum` < '2020-12-31'
        AND `betalingsnr` > 0;
    
 */